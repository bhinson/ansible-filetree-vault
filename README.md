Extract credentials from data exported using filetree_create
<p>
usage: ansible-playbook extract-creds-to-vault.yml -e '{ target_dir: /path/to/filetree/output }'
(output filename: remotevault.yml)
</p>
